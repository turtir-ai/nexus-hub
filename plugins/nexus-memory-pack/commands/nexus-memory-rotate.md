---
description: Rotate oversized memory_bank into archives and rebuild. Use --force to archive immediately.
argument-hint: [--force]
allowed-tools: [Bash]
---

# /nexus-memory-rotate

User arguments: $ARGUMENTS

## Your task

1. Run:
```bash
python3 ${CLAUDE_PLUGIN_ROOT}/nexus_mem.py rotate $ARGUMENTS
```
2. Print the JSON output only.

