---
description: Import a NEXUS memory bundle (tar.gz). Merges notes; use --overwrite-pinned if desired.
argument-hint: [--overwrite-pinned] <bundle.tar.gz>
allowed-tools: [Bash]
---

# /nexus-memory-import

User arguments: $ARGUMENTS

## Your task

1. Run:
```bash
python3 ${CLAUDE_PLUGIN_ROOT}/nexus_mem.py import $ARGUMENTS
```
2. Print the JSON output only.

