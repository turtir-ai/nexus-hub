---
description: Export a NEXUS memory bundle (tar.gz) so you can move memory to another machine
argument-hint: [--out <path>] [--with-archives] [--with-state]
allowed-tools: [Bash]
---

# /nexus-memory-export

User arguments: $ARGUMENTS

## Your task

1. Run:
```bash
python3 ${CLAUDE_PLUGIN_ROOT}/nexus_mem.py export $ARGUMENTS
```
2. Print the JSON output only.

